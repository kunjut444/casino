# 🎰 Мини Казино - Full Stack приложение

Полнофункциональное приложение для мини-казино с Spring Boot бэкендом и React фронтендом.

## 📁 Структура проекта

```
casino/
├── src/                    # Spring Boot бэкенд
│   └── main/java/org/example/
│       ├── controller/     # REST API контроллеры
│       ├── service/        # Бизнес-логика
│       ├── entity/         # JPA сущности
│       ├── repository/     # Репозитории
│       ├── dto/            # Data Transfer Objects
│       └── security/       # Конфигурация безопасности и CORS
└── frontend/               # React фронтенд
    └── src/
        ├── components/     # React компоненты
        ├── services/       # API сервисы
        ├── context/        # React Context (Auth)
        └── types/          # TypeScript типы
```

## 🚀 Запуск проекта

### Полная интеграция (Бэкенд + Фронтенд на одном порту)

1. Убедитесь, что у вас установлен Java 17 и Maven
2. Запустите приложение:
```bash
mvn spring-boot:run
```

3. Откройте браузер и перейдите на:
```
http://localhost:8081
```

**Всё!** Фронтенд и бэкенд работают на одном порту `8081`. Фронтенд автоматически обслуживается Spring Boot из `src/main/resources/static/`.

### API Endpoints

Все API доступны по адресу `http://localhost:8081/api/...`:
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `POST /api/bets` - Сделать ставку
- `GET /api/players` - Получить всех игроков
- и т.д.

### Альтернативный способ (отдельный фронтенд)

Если хотите запустить фронтенд отдельно (для разработки):
1. Откройте файл `casino.html` в браузере (двойной клик)
2. Или используйте папку `frontend` с Node.js (см. ниже)

**С Node.js (опционально):**
```bash
cd frontend
npm install
npm run dev
```
Фронтенд будет доступен на `http://localhost:3000` (требует запущенный бэкенд на 8081)

## 🎮 Функциональность

### Аутентификация
- Регистрация новых пользователей
- Вход в систему
- Автоматическое сохранение сессии в localStorage

### Типы ставок

1. **Четное/Нечетное (EVEN_ODD)**
   - Выбор между четным и нечетным числом
   - Выигрыш: удвоенная ставка

2. **Числовая Рулетка (NUMBER_ROULETTE)**
   - Выбор числа от 1 до 36
   - Выигрыш: в 35 раз больше ставки

3. **Эмодзи Рулетка (EMOJI_ROULETTE)**
   - Выбор одного из 8 эмодзи
   - Выигрыш: в 7 раз больше ставки

### Дополнительные возможности
- Отображение текущего баланса игрока
- История всех ставок
- Быстрый выбор суммы ставки (25%, 50%, 75%, Все)
- Адаптивный дизайн для мобильных устройств
- Современный UI с градиентами и анимациями

## 🛠 Технологии

### Бэкенд
- Spring Boot 3.2.0
- Spring Data JPA
- Spring Security
- H2 Database (in-memory)
- Lombok
- Maven

### Фронтенд
- React 18
- TypeScript
- Vite
- React Router
- Axios
- CSS3

## 📡 API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход

### Ставки
- `POST /api/bets` - Сделать ставку

### Игроки
- `GET /api/players` - Получить всех игроков
- `GET /api/players/{id}` - Получить игрока по ID
- `POST /api/players` - Создать игрока
- `PUT /api/players/{id}` - Обновить игрока
- `DELETE /api/players/{id}` - Удалить игрока

## 💡 Примечания

- При регистрации новый игрок получает стартовый баланс $1000
- База данных H2 создается автоматически при запуске
- CORS настроен для работы фронтенда с бэкендом
- Vite proxy настроен для проксирования API запросов

## 🎨 Скриншоты

Приложение имеет современный дизайн с:
- Градиентными фонами
- Плавными анимациями
- Адаптивной версткой
- Интуитивным интерфейсом

---

Удачи в игре! 🎲

